---
import Layout from '../../layouts/Layout.astro';
import Intro from '../../components/common/Intro.astro';
import Content from '../../components/common/Content.astro';
import { getPostsByType } from '../../lib/content';

const blogPosts = await getPostsByType('blog');
---

<Layout pageId="blogs">
  <main>
    <section class="intro">
      <Intro
        eyebrow="Things I'm thinking about."
        hero="BLOGS"
        lead="A place to think out loud."
        body="Notes, observations, and ideas from life and everything in between."
      />
    </section>

    <Content posts={blogPosts} showContentType={false} />
  </main>
</Layout>

<style>
  main {
    width: min(768px, 100%);
    margin: 0 auto;
  }
</style>
